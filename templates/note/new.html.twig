{% extends 'base.html.twig' %}

{% block title %}Создание новой заметки{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Создание новой заметки</h1>

        <div class="row">
            <div class="col-md-8 offset-md-2">
                <!-- Форма создания новой заметки -->
                <div class="card mb-4">
                    <div class="card-body">
                        {{ include('note/_form.html.twig', {'button_label': 'Create', 'button_class': 'btn btn-success'}) }}
                    </div>
                </div>

                <!-- Кнопка возврата к списку -->
                <div class="d-flex justify-content-between">
                    <a href="{{ path('app_note_index') }}" class="btn btn-secondary">Вернуться к списку</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
